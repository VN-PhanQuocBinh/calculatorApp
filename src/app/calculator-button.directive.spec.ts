import { CalculatorButtonDirective } from './calculator-button.directive';

describe('CalculatorButtonDirective', () => {
  it('should create an instance', () => {
    const directive = new CalculatorButtonDirective();
    expect(directive).toBeTruthy();
  });
});
